# Stage 12 — Risk Committee Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`. Checkboxes for tracking.

**Goal:** Stand up the `risk` dept-orchestrator (CRO Agent + 3 specialists) using existing scaffold.

**Spec:** `docs/superpowers/specs/2026-04-28-stage12-risk-design.md`
**Framework:** `docs/superpowers/specs/2026-04-28-stage10-phase2-framework-design.md`
**Posture:** `read_only` · **Cross-reads:** cac, cio, finance, legal · **Existing scaffold:** ✅ `skills/risk/` (4 files) · **Dependencies:** Stage 10; cross-read targets graceful-degrade if Stage 13/17 not yet live · **Service port:** 3011

**Status:** skeleton

---

## Pre-flight

- [ ] Stage 10 + Stage 11 (Finance) live
- [ ] `skills/risk/` exists with 4 scaffold files (risk-orchestrator, credit-risk, market-risk, operational-risk)
- [ ] `config/departments.json#risk` row complete

---

## Phase A — Catalog audit + spec finalization

### Task A.1: Audit existing scaffold
- [ ] Review `skills/risk/*.md` content quality
- [ ] Confirm rename: `risk-orchestrator.md` → `cro-agent.md` for naming consistency (or keep — decision at kickoff)
- [ ] Verify frontmatter has Stage 10 permissions block (added during Stage 10 Task 2.7)

### Task A.2: Finalize spec
- [ ] Brainstorm: §4 escalation thresholds (sourced from existing risk policy doc), §7 HOD email
- [ ] Decision: AML co-ownership detail — Legal owns the doc, Risk has crossRead — confirm spec wording matches
- [ ] Spec review until approved

---

## Phase B — Scaffold

### Task B.1: Service scaffold (port 3011)
- [ ] `service-scaffold` from `services/_template-orchestrator/` → `services/risk-orchestrator/`
- [ ] Replace placeholders: DEPT_ID=risk, DEPT_NAME=Risk, PORT=3011

### Task B.2: Adopt skills (already exist)
- [ ] Audit `skills/risk/credit-risk.md`, `market-risk.md`, `operational-risk.md` against spec §1
- [ ] Update permissions frontmatter: `mode: read_only`, `read_collections: [risk_docs, risk_chat, risk_knowledge, shared_policies, cac_docs, cio_docs, finance_docs, legal_docs]`
- [ ] No skill-writer needed unless content gaps identified

---

## Phase C — Wiring

### Task C.1: Specialists + read-only LangGraph
- [ ] 4 Python agent classes
- [ ] **Drop staging-path nodes** at graph compile time (read-only dept)
- [ ] Standard graph: load_memory → classify → retrieve → specialist → escalation → synthesise → log → paperclip
- [ ] `--print-graph` confirms NO `staging_writer` node

### Task C.2: Migration
- [ ] `migrations/012_add_risk_department.sql` — paperclip_departments + 4 paperclip_agents

---

## Phase D — Tests

### Task D.1: Unit
- [ ] Standard 3 unit test files
- [ ] **Test that read-only enforcement is real:** assert graph has no staging_writer; assert `ensure_can_write` raises for risk skills

### Task D.2: Integration
- [ ] `test_risk_e2e.py` — query-only flow (no staging proposal possible)
- [ ] `test_risk_cross_dept_read.py` — Risk Agent retrieves from `cac_docs` ✅, `finance_docs` ✅, `legal_docs` (graceful degrade if Stage 13 not yet live), `cio_docs` (graceful degrade until Stage 17)
- [ ] `test_risk_reflection.py` — reflection engine processes risk daily log

### Task D.3: Coverage ≥ 80%

---

## Phase E — Deploy

### Task E.1: docker-compose (port 3011, **no staging:rw mount**)
### Task E.2: `#risk-committee` Slack channel
### Task E.3: Vault folder (already created in Stage 10) + author 4 soul.md files

---

## Phase F — Smoke + Go-live

### Task F.1: 6 smoke tests; cross-read graceful degrade verified
### Task F.2: Live=true, restart gateway
### Task F.3: `git tag stage-12-risk-live`

---

## Stage-specific acceptance criteria *(spec §9)*

- [ ] All 6 framework smoke tests pass
- [ ] Cross-read works: CRO Agent retrieves from `cac_docs`, `cio_docs`, `finance_docs`, `legal_docs` (graceful-degrade for not-yet-live targets)
- [ ] Read-only enforcement: `staging_writer` node absent from compiled graph (verified `--print-graph`)
- [ ] Existing `skills/risk/` content audited and merged with framework permissions frontmatter
- [ ] Risk policy queries return citations from `risk_docs`

## Effort

~1.5 days. Has scaffold so faster on skill drafting; read-only so no staging integration testing.

## Parallelisation note

Stages 12, 13, 14 can run in parallel post-Stage-11 if review bandwidth allows (per framework §7.4).
