# Stage 18 — VCC Department Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.

**Goal:** Stand up the `vcc` dept-orchestrator (VCC Head Agent + 3 specialists). Write-capable for NAV + client list updates via staging. Fresh design, no scaffold.

**Spec:** `docs/superpowers/specs/2026-04-28-stage18-vcc-design.md`
**Framework:** `docs/superpowers/specs/2026-04-28-stage10-phase2-framework-design.md`
**Posture:** `write` · **Cross-reads:** cio, ic · **Existing scaffold:** ❌ none · **Dependencies:** Stage 17 (CIO) MUST be live; Stage 16 (IC) recommended · **Service port:** 3017

**Status:** skeleton

---

## Pre-flight

- [ ] Stages 10 + 17 live
- [ ] `skills/vcc/` exists but empty
- [ ] `obsidian-vault/vcc/` skeleton exists
- [ ] Per-skill namespace decision (`comms-agent` collision with Stage 15) resolved — see Phase B.2

---

## Phase A — Catalog audit + spec finalization

### Task A.1: Audit
- [ ] Verify 8 docs in inventory (client list, presentations, contracts, due diligence, subscriptions, NAV, newsletters, investment memos)

### Task A.2: Finalize spec
- [ ] Brainstorm: §4 escalation thresholds (subscription anomaly, contract expiring), §7 HOD email
- [ ] **Decision:** add custom `kyc_check` node before retrieval for client-relations queries? Default: defer to Stage 18.5
- [ ] **Decision (per spec review recommendation 4):** namespace scheme for `comms-agent` (Stage 15 has same identifier). Pin scheme: `paperclip_agents` row uses composite key `{dept_id}:{agent_name}` for uniqueness. Document in migration.
- [ ] Spec review

---

## Phase B — Scaffold

### Task B.1: Service scaffold (port 3017)
### Task B.2: Skills (4 fresh files; namespacing-aware)
- [ ] `skill-writer` for `vcc-head-agent.md`, `client-relations.md`, `nav-subs.md`, `comms.md` (VCC-internal, distinct from Stage 15 Comms dept)
- [ ] Default frontmatter: `mode: write_via_staging`, `data_zones: [1, 2]`, `read_collections: [vcc_docs, vcc_chat, vcc_knowledge, shared_policies, cio_docs, ic_docs]`
- [ ] **Override client-relations.md:** PII guard — `outbound_apis: []` even though dept allows others (per spec §6)

---

## Phase C — Wiring

### Task C.1: Specialists + write LangGraph
- [ ] 4 agent classes; classify_intent routes client queries to client-relations, NAV queries to nav-subs, etc.
- [ ] Custom `kyc_check` if decided
- [ ] Verify client-relations `outbound_apis: []` blocks heartbeat outbound for that skill

### Task C.2: Migration with namespacing
- [ ] `migrations/018_add_vcc_department.sql`:
  - INSERT `paperclip_departments` (`vcc`, ...)
  - INSERT `paperclip_agents` with composite key (avoid collision with Stage 15 `comms-agent`)
  - Document the namespace convention in migration comment for future stages

---

## Phase D — Tests

### Task D.1: Unit (4 agents)

### Task D.2: Integration
- [ ] `test_vcc_e2e.py` — NAV staging proposal flow (parallel to Stage 17 CIO flow but for VCC NAV)
- [ ] `test_vcc_cross_dept_read.py` — VCC Agent retrieves from `cio_docs` (Stage 17 must be live), `ic_docs` (if Stage 16 live)
- [ ] `test_vcc_client_list_update.py` — client list staging proposal flow (rare; tracker-tier doc)
- [ ] `test_vcc_pii_guard.py` — **critical:** client-relations skill cannot trigger any outbound action (verifies `outbound_apis: []` enforcement)
- [ ] `test_vcc_reflection.py`

### Task D.3: Coverage ≥ 80%

---

## Phase E — Deploy

### Task E.1: docker-compose port 3017, **with staging:rw**
### Task E.2: `#vcc-committee` Slack
### Task E.3: Vault + 4 soul.md (VCC Head: client-centric; specialists: relationship-precision, NAV-accuracy, content-voice)

---

## Phase F — Smoke + Go-live

### Task F.1: 6 smoke tests + NAV flow + PII guard verified
### Task F.2: Live=true; **CIO's cross-read to vcc_docs becomes real (no longer graceful-degrade)**
### Task F.3: `git tag stage-18-vcc-live`

---

## Stage-specific acceptance criteria *(spec §9)*

- [ ] All 6 framework smoke tests pass
- [ ] Cross-read: VCC retrieves from `cio_docs` (must be live) and `ic_docs` (if live)
- [ ] NAV staging proposal flow works (mirror of CIO flow)
- [ ] Client list update staging proposal flow works (rare event; tracker-tier doc)
- [ ] **PII handling:** client-relations skill cannot trigger any outbound action (verified by integration test)
- [ ] CIO Agent (Stage 17) can RAG-retrieve from `vcc_docs` for fund alignment

## Effort

~1.5 days. PII guard + namespacing are the new pieces.
