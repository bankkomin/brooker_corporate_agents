# Stage 16 — IC Committee Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.

**Goal:** Stand up the `ic` dept-orchestrator (IC Chair Agent + 3 specialists). Existing scaffold renamed `skills/invest/` → `skills/ic/` in Stage 10.

**Spec:** `docs/superpowers/specs/2026-04-28-stage16-ic-design.md`
**Framework:** `docs/superpowers/specs/2026-04-28-stage10-phase2-framework-design.md`
**Posture:** `read_only` · **Cross-reads:** finance, cio, vcc, legal · **Existing scaffold:** ✅ `skills/ic/` (renamed) · **Dependencies:** Stages 11 (Finance) + 13 (Legal) MUST be live; CIO/VCC graceful-degrade until Stages 17/18 · **Service port:** 3015

**Status:** skeleton

---

## Pre-flight

- [ ] Stages 10 + 11 + 13 live
- [ ] `skills/ic/*.md` exists (renamed from invest/ in Stage 10)
- [ ] Framework spec §3.5 graceful-degrade rule in place (handles `cio_docs`/`vcc_docs` not yet existing)

---

## Phase A — Catalog audit + spec finalization

### Task A.1: Audit
- [ ] Rename decision: `ic-orchestrator.md` → `ic-chair-agent.md` for naming consistency
- [ ] Confirm 3 docs in inventory (Investment policy, IC minutes, Investment memos)

### Task A.2: Finalize spec
- [ ] Brainstorm: §4 escalation rules (investment policy breach, IC vote ready, concentration), §7 HOD email
- [ ] **Decision:** add custom `valuation_check` node? If yes, document in spec §3 + plan accordingly
- [ ] Spec review

---

## Phase B — Scaffold

### Task B.1: Service scaffold (port 3015)
### Task B.2: Adopt + update skills
- [ ] Frontmatter: `read_only`, `read_collections: [ic_docs, ic_chat, ic_knowledge, shared_policies, finance_docs, cio_docs, vcc_docs, legal_docs]`

---

## Phase C — Wiring

### Task C.1: Read-only graph
- [ ] If `valuation_check` custom node decided: insert after specialist, before escalation
### Task C.2: `migrations/016_add_ic_department.sql`

---

## Phase D — Tests

### Task D.1: Unit (4 agents + custom node if added)

### Task D.2: Integration
- [ ] `test_ic_e2e.py` — read-only query flow
- [ ] `test_ic_cross_dept_read.py` — verifies retrieval from `finance_docs` ✅, `legal_docs` ✅; **graceful-degrade** for `cio_docs`/`vcc_docs` (framework §3.5 — return zero hits, no error)
- [ ] `test_ic_valuation_check.py` (if custom node added)
- [ ] `test_ic_reflection.py`

### Task D.3: Coverage ≥ 80%

---

## Phase E — Deploy

### Task E.1: docker-compose port 3015, no staging:rw
### Task E.2: `#ic-committee` Slack
### Task E.3: Vault + 4 soul.md (IC Chair: judicious; specialists: rigorous DD + valuation methodology)

---

## Phase F — Smoke + Go-live

### Task F.1: 6 smoke tests; graceful-degrade for cio/vcc verified
### Task F.2: Live=true
### Task F.3: `git tag stage-16-ic-live`

---

## Stage-specific acceptance criteria *(spec §9)*

- [ ] All 6 framework smoke tests pass
- [ ] Read-only enforcement: `staging_writer` node absent
- [ ] Cross-read works for `finance_docs` and `legal_docs` (Stages 11 + 13 already live)
- [ ] **Cross-read graceful-degrade:** missing `cio_docs` / `vcc_docs` collections do not error retrieval (returns empty hits, agent answers from available)
- [ ] Investment policy queries return citations from `ic_docs`
- [ ] Existing `skills/ic/` content audited with framework frontmatter

## Effort

~1.5 days. Has scaffold; cross-read graceful-degrade testing is the new piece.

## Decision gate after Stage 16

Per framework §7.5: pause to evaluate; remaining 3 stages (17 CIO, 18 VCC, 19 IB) are higher complexity.
