# Stage 17 — CIO Office Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.

**Goal:** Stand up the `cio` dept-orchestrator (CIO Agent + 3 specialists). Write-capable for NAV trackers via staging gate. Fresh design, no scaffold.

**Spec:** `docs/superpowers/specs/2026-04-28-stage17-cio-design.md`
**Framework:** `docs/superpowers/specs/2026-04-28-stage10-phase2-framework-design.md`
**Posture:** `write` · **Cross-reads:** finance, vcc, ic · **Existing scaffold:** ❌ none · **Dependencies:** Stage 11 (Finance) MUST be live; Stage 16 (IC) recommended · **Service port:** 3016

**Status:** skeleton

---

## Pre-flight

- [ ] Stages 10 + 11 live
- [ ] `obsidian-vault/cio/` skeleton exists from Stage 10
- [ ] `skills/cio/` exists but empty (Stage 10 created folder)
- [ ] Custodian-statement ingestion source identified (read-only via sync-mirror)

---

## Phase A — Catalog audit + spec finalization

### Task A.1: Audit
- [ ] Verify 3 docs in inventory (NAV reports, PPM, Custodian docs)
- [ ] Confirm `capabilityTier: write` enables staging path

### Task A.2: Finalize spec
- [ ] Brainstorm: §4 escalation thresholds (nav_discrepancy tolerance %, recon failure triggers), §7 HOD email, §3 custom `custodian_check` node decision
- [ ] **Important:** custody-agent skill must enforce `data_zones: [1]` override (per spec §6) even though dept is write-capable
- [ ] Spec review

---

## Phase B — Scaffold

### Task B.1: Service scaffold (port 3016)
### Task B.2: Skills (4 fresh files)
- [ ] `skill-writer` for `cio-agent.md`, `fund-performance.md`, `custody.md`, `ppm.md`
- [ ] Default frontmatter: `mode: write_via_staging`, `data_zones: [1, 2]`, `read_collections: [cio_docs, cio_chat, cio_knowledge, shared_policies, finance_docs, vcc_docs, ic_docs]`
- [ ] **Override custody.md:** `data_zones: [1]` (read-only on Zone 2 for custodian data — see spec §6 override table)

---

## Phase C — Wiring

### Task C.1: Specialists + write LangGraph
- [ ] 4 agent classes
- [ ] Custom `custodian_check` node (if decided in A.2): position after specialist, before escalation; verifies NAV proposal against latest custodian doc before allowing staging
- [ ] Standard write-path nodes intact (excel_navigator, validate_proposal, staging_writer)
- [ ] Verify custody-agent's `data_zones: [1]` blocks it from reaching staging_writer (integration test)

### Task C.2: Migration
- [ ] `migrations/017_add_cio_department.sql` — depts + 4 agents

---

## Phase D — Tests

### Task D.1: Unit (4 agents + custodian_check if added)

### Task D.2: Integration
- [ ] `test_cio_e2e.py` — NAV staging proposal flow: agent proposes → custodian_check (if added) → HOD email → approval-ui → sync-back writes NAV mirror
- [ ] `test_cio_cross_dept_read.py` — `finance_docs` ✅, `vcc_docs` graceful-degrade (Stage 18 not yet live), `ic_docs` ✅ (if Stage 16 live)
- [ ] `test_cio_custody_zone_enforcement.py` — **critical:** custody-agent skill cannot reach staging_writer; integration test verifies the `data_zones: [1]` override is enforced
- [ ] `test_cio_ppm_query.py` — PPM update query returns latest version
- [ ] `test_cio_reflection.py`

### Task D.3: Coverage ≥ 80%

---

## Phase E — Deploy

### Task E.1: docker-compose port 3016, **with staging:rw mount**
### Task E.2: `#cio-committee` Slack
### Task E.3: Vault + 4 soul.md (CIO Agent: investment-thesis articulation; specialists: NAV precision, custody reconciliation, PPM compliance)

---

## Phase F — Smoke + Go-live

### Task F.1: 6 smoke tests + NAV staging flow + custody zone enforcement
### Task F.2: Live=true
### Task F.3: `git tag stage-17-cio-live`

---

## Stage-specific acceptance criteria *(spec §9)*

- [ ] All 6 framework smoke tests pass
- [ ] Cross-read works: CIO Agent retrieves from `finance_docs` (always) and `vcc_docs`/`ic_docs` if those stages are live
- [ ] NAV staging proposal flow: agent proposes value → HOD email → approval-ui → approve → sync-back writes NAV tracker mirror
- [ ] **Custody-agent skill enforces `data_zones: [1]` override** — cannot reach staging_writer (integration test)
- [ ] PPM update queries surface latest version from `cio_docs`
- [ ] VCC Agent (post Stage 18) can RAG-retrieve from `cio_docs` for fund alignment

## Effort

~1.5 days. Per-skill data_zone override is the new wrinkle.

## Note for Stage 18

Once Stage 17 lands, Stage 18 VCC's cross-read to `cio_docs` is no longer graceful-degrade — must be a real test.
