# Stage 14 — IT Department Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.

**Goal:** Stand up the `it` dept-orchestrator (CTO Agent + 3 specialists) using existing scaffold. CTO Agent identity already exists in Paperclip from Stage 8 (OpenClaw supervisor) — must reuse, not duplicate.

**Spec:** `docs/superpowers/specs/2026-04-28-stage14-it-design.md`
**Framework:** `docs/superpowers/specs/2026-04-28-stage10-phase2-framework-design.md`
**Posture:** `read_only` · **Cross-reads:** own + shared only (standard isolation) · **Existing scaffold:** ✅ `skills/it/` · **Dependencies:** Stage 10 only · **Service port:** 3013

**Status:** skeleton

---

## Pre-flight

- [ ] Stage 10 live
- [ ] `skills/it/*.md` (it-orchestrator, devops, infrastructure, security) exists
- [ ] `paperclip_agents` table has existing `cto-agent` row from Stage 8 — note its current `agent_id` for Step C.2

---

## Phase A — Catalog audit + spec finalization

### Task A.1: Audit
- [ ] Rename decision: `it-orchestrator.md` → `cto-agent.md` (per spec §1) — note this changes the agent identity that Stage 8 OpenClaw heartbeats to
- [ ] Verify 2 docs in inventory (IT policies, IT SOPs)

### Task A.2: Finalize spec
- [ ] Brainstorm: §4 escalation rules (security incident, policy breach), §7 HOD email
- [ ] **Important:** plan the CTO Agent identity migration — see Task C.2

---

## Phase B — Scaffold

### Task B.1: Service scaffold (port 3013)
### Task B.2: Adopt + update skills
- [ ] Frontmatter: `read_only`, `read_collections: [it_docs, it_chat, it_knowledge, shared_policies]`

---

## Phase C — Wiring

### Task C.1: Read-only graph

### Task C.2: Migration with **identity continuity**
- [ ] `migrations/014_add_it_department.sql`:
  - INSERT `paperclip_departments` (`it`, ...)
  - **UPDATE existing `cto-agent` row in `paperclip_agents`** to set `dept_id='it'` (don't INSERT — old row from Stage 8 must continue)
  - INSERT 3 specialist rows (devops, infrastructure, security)
- [ ] Verify OpenClaw still heartbeats successfully against the migrated CTO Agent row

---

## Phase D — Tests

### Task D.1: Unit (4 agents)

### Task D.2: Integration
- [ ] `test_it_e2e.py` — read-only query flow
- [ ] `test_it_cto_identity_continuity.py` — **critical:** OpenClaw's heartbeat from Stage 8 still resolves; no orphaned old `cto-agent` row
- [ ] `test_it_reflection.py`

### Task D.3: Coverage ≥ 80%

---

## Phase E — Deploy

### Task E.1: docker-compose port 3013, no staging:rw
### Task E.2: `#it-committee` Slack
### Task E.3: Vault + 4 soul.md (CTO Agent reuses tone from existing Stage 8 OpenClaw supervisor persona)

---

## Phase F — Smoke + Go-live

### Task F.1: 6 smoke tests + OpenClaw still functions correctly
### Task F.2: Live=true
### Task F.3: `git tag stage-14-it-live`

---

## Stage-specific acceptance criteria *(spec §9)*

- [ ] All 6 framework smoke tests pass
- [ ] Read-only enforcement: `staging_writer` node absent from compiled graph
- [ ] **CTO Agent identity continuity:** heartbeat to Paperclip uses the migrated agent_id row (no orphaned Stage 8 entry)
- [ ] Existing `skills/it/` content audited with framework frontmatter
- [ ] IT policy queries return citations from `it_docs`

## Effort

~1.5 days. Identity continuity migration is the only non-mechanical bit.

## Parallelisation

Can run alongside Stages 12 + 13.
