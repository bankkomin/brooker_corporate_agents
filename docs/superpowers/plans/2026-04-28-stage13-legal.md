# Stage 13 — Legal Department Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`.

**Goal:** Stand up the `legal` dept-orchestrator (CLO Agent + 3 specialists) using existing scaffold. Legal has cross-read access to **all** departments — broadest read scope in the system.

**Spec:** `docs/superpowers/specs/2026-04-28-stage13-legal-design.md`
**Framework:** `docs/superpowers/specs/2026-04-28-stage10-phase2-framework-design.md`
**Posture:** `read_only` · **Cross-reads:** **all** · **Existing scaffold:** ✅ `skills/legal/` · **Dependencies:** Stage 10 + 11 (Finance contracts cross-ref); 12 (Risk co-owned AML) helpful · **Service port:** 3012

**Status:** skeleton

---

## Pre-flight

- [ ] Stage 10 + 11 live
- [ ] `skills/legal/*.md` (legal-orchestrator, compliance, contract-review, regulatory) exists
- [ ] AML co-ownership convention: Legal owns the doc; Risk has cross-read — verify in inventory

---

## Phase A — Catalog audit + spec finalization

### Task A.1: Audit existing scaffold
- [ ] Review skill content quality; rename `legal-orchestrator.md` → `clo-agent.md` (decision at kickoff)
- [ ] Verify all 3 documents in inventory (Legal opinions, AML policy, Contract templates)

### Task A.2: Finalize spec
- [ ] Brainstorm: §4 escalation rules (regulatory, AML, contract red-flag), §7 HOD email
- [ ] **Important:** crossRead `["*"]` resolution — confirm `retrieve_context_with_crossread` expands `*` to all live depts at query time
- [ ] Spec review

---

## Phase B — Scaffold

### Task B.1: Service scaffold (port 3012)
### Task B.2: Adopt + update skills
- [ ] Update frontmatter: `mode: read_only`, `read_collections: ["*"]` (resolved at runtime to all `_docs` collections + shared_policies)

---

## Phase C — Wiring

### Task C.1: Read-only graph (no staging_writer)
### Task C.2: `migrations/013_add_legal_department.sql`

---

## Phase D — Tests

### Task D.1: Unit (4 specialists)

### Task D.2: Integration
- [ ] `test_legal_e2e.py` — read-only query flow
- [ ] `test_legal_cross_dept_read.py` — **Legal Agent can retrieve from EVERY `_docs` collection** (most expansive cross-read test in the system; verifies `["*"]` expansion)
- [ ] `test_legal_aml_co_ownership.py` — query about AML returns citations from BOTH Legal (owner) and Risk (cross-read), de-duplicated
- [ ] `test_legal_reflection.py`

### Task D.3: Coverage ≥ 80%

---

## Phase E — Deploy

### Task E.1: docker-compose port 3012, no staging:rw
### Task E.2: `#legal-committee` Slack
### Task E.3: Vault + 4 soul.md files (Legal Agent: cautious, precise, citation-heavy)

---

## Phase F — Smoke + Go-live

### Task F.1: 6 smoke tests + cross-read all-depts verified
### Task F.2: Live=true
### Task F.3: `git tag stage-13-legal-live`

---

## Stage-specific acceptance criteria *(spec §9)*

- [ ] All 6 framework smoke tests pass
- [ ] **Cross-read all-depts works:** Legal Agent retrieves from any `*_docs` collection per `crossReadAccess: ["*"]`
- [ ] AML co-ownership: Risk Agent and Legal Agent both surface AML policy on relevant queries (de-duplicated)
- [ ] Contract review query returns citations from cross-dept collections (e.g. asks about HR contract → retrieves from `hr_docs`)
- [ ] Existing `skills/legal/` content audited and merged with framework permissions frontmatter

## Effort

~1.5 days. Read-only + scaffold = faster drafting. The `["*"]` cross-read makes integration testing slightly larger.

## Parallelisation

Can run alongside Stages 12 + 14.
